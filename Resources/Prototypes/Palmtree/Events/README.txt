This folder contains all Palmtree's unique admin-driven events.
They are self-contained here, so each folder is everything the event needs to function.

Inside the "shared" file is where you'll find stuff regarding the events in general, such as base prototypes definitions and other stuff that is shared across all events.